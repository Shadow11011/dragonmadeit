/**
 * Legacy fire-ember decorative background. Disabled in the rebrand —
 * the mature SaaS aesthetic uses flat surfaces, no floating particles.
 * Component kept as a no-op so any stray import doesn't break the build.
 */
export function EmberBackground() {
  return null;
}
